import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  appContainer: {
    flex: 1,
    paddingTop: 300,
    fontSize: 30,
  },
  text: {
    color: "red",
    fontSize: 30,
  },
});

export default styles;
